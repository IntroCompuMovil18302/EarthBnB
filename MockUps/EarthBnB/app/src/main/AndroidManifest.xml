<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.edu.javeriana.earthbnb">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity" />
        <activity android:name=".Reservas" />
        <activity android:name=".CalificacionCliente" />
        <activity android:name=".ConsultaReservas" />
        <activity android:name=".prueba" />
        <activity android:name=".VerPerfilActivity" />
        <activity
            android:name=".EditarPerfilActivity"
            android:label="@string/title_activity_editar_perfil"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>